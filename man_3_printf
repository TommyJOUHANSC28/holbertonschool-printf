.TH_PRINTF 3 "27 November 2025" "Version 1.0" "User Commands"
.SH NAME
_printf \-produces output according to a format

.SH SYONPSIS
.B int_printf(const char *format, ...);

.SH DESCRIPTION 
The function
.B _printf
produces output according to a format string passed as first argument.
The format string is composed of zero or more directives. Each directive starts with a '%' character followed by a conversion specifier.

The following conversion specifiers are handled:

.TP
.B c
Prints a single character.

.TP
.B s
Prints a string of characters.

.TP
.B %
Prints a percent sign.
.TP 
.B d, i 
Prints a signed integer in decimal.

.SH RETURN VALUE
On success, returns the total number of characters printed (excluding the terminating null byte).
On failure (for example if format is NULL), reurns -1.

.BUGS
No detected so far, Please report any to 12119@holbertonstudents.com

.SH AUTHOR
Tommy JOUHANS (github: tommyJOUHANSC28), Holberton School from Dijon, in FRANCE.
Maxence  HENOUS (github: maxence30), Holberton School from Dijon, in FRANCE.

.DATE
27th November 2025

.SEE ALSO
printf(3)
